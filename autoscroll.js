function pobieranie() {
    document.querySelector('.pobieranie').scrollIntoView({ block: 'start',  behavior: 'smooth' });
};

function gora() {
    document.querySelector('body').scrollIntoView({ block: 'start',  behavior: 'smooth' });
};